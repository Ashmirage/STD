# AGENTS.md

## 任务说明
- 我要准备一个单片机竞赛,比赛时长4个小时,现在是比赛前的准备阶段,准备好清晰可以直接照抄的代码框架,比赛允许携带纸质资料,不允许联网.

## 项目说明
- 这是一个 STM32F407ZGT6 的嵌入式工程，使用 STM32CubeMX + Keil MDK-ARM。
- 不要假设仓库中存在 Makefile、CMake、pytest 或其他本地自动化构建脚本。
- 这里只作为代码编辑区域,我烧录在其他地方做

## 代码修改规则
- `Core/*` 和 `gpio.c`、`usart.c`、`rtc.c`、`fsmc.c` 等外设初始化文件视为 CubeMX 管理文件。
- 如需手动修改，优先放在 `/* USER CODE BEGIN ... */` 块中，避免被重新生成覆盖。
- 如需修改引脚、时钟、外设配置，应优先说明是否需要更新 `.ioc`。
- 新增 BSP/SYS 文件时，不要默认已经参与编译；需要提醒用户把文件加入 `F407ZGT6_HAL.uvprojx`。

## 回答风格
- 优先使用中文回答。
- 优先给最小修改方案，不要随意大范围重构。
- 给出代码时，明确说明应放在哪个文件、哪个位置。

## 执行要求
- 先给出方案,不要直接修改代码
- 每次修改完代码之后,告知我代码使用的什么方法,以及修改位置